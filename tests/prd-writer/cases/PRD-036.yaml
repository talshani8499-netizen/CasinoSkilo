id: PRD-036
category: handoff
title: "Bet on it + atlassian MCP → Confluence create call with PKB params"
references:
  - skill: prd-writer
  - skill_md_lines: [181, 190]
preconditions:
  user_input: "PRD for a new VIP tier upgrade celebration screen — Bet on it"
  jira_context: |
    PR-415 / open / "VIP tier UX refresh" / component: vip
  confluence_context: null
  active_memory: null
  mcp_state: atlassian
pass_criteria:
  - id: mcp-tool-lookup-pattern
    target: "confluence-create-page"
    expected_regex: 'mcp__.*(atlassian|confluence).*'
    expected_verbs: ["createPage", "createConfluencePage", "create_page"]
  - id: confluence-call-uses-pkb-params
  - id: markdown-saved-with-absolute-path
    folder: "prds"
notes: "Bet on it with atlassian MCP must call Confluence create-page with spaceId=772571142, parentId=772571521, contentFormat=markdown, and the artifact title."
