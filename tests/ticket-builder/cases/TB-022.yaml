id: TB-022
category: phase-c-followups
title: "Phase C falls back to assume + flag when 3 concrete options can't be written (rule 6)"
references:
  - skill: ticket-builder
  - skill_md_lines: [103, 103]
preconditions:
  user_input: "add a subtle entrance animation when the VIP bar mounts"
  jira_context: |
    PR-470 / closed / "Casino home redesign" / component: home / file: components/home/banner.tsx
  confluence_context: null
  active_memory: null
  mcp_state: atlassian
pass_criteria:
  - id: phase-c-fallback-to-assume-and-flag
    topic: "preferred animation library"
  - id: phase-c-no-banned-placeholders
    banned_phrases:
      - "Use the existing animation system"
      - "Option A"
      - "TBD"
notes: "Rule 6: no concrete options exist for animation library choice (no signal in context). Skill must NOT ask — it converts the unknown to an inline assumption + flag instead of inventing placeholders."
