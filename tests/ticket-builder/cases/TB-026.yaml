id: TB-026
category: output-structure
title: "Artifact omits all banned sections (Background, Context, Problem, Scope, FRs, etc.)"
references:
  - skill: ticket-builder
  - skill_md_lines: [119, 162]
preconditions:
  user_input: "add a VIP progress bar to the casino home — players need to see how close they are to the next tier so they keep depositing"
  jira_context: |
    PR-470 / closed / "Casino home redesign" / component: home / file: components/home/banner.tsx
    PR-481 / open / "Smartico LOYALTY_POINTS_UPDATE wiring" / component: integrations
  confluence_context: |
    Page: "VIP Program v3 PRD"
  active_memory: null
  mcp_state: atlassian
pass_criteria:
  - id: output-omits-banned-sections
    banned_sections:
      - "Background"
      - "Context"
      - "Problem"
      - "Desired Outcome"
      - "Scope"
      - "Functional Requirements"
      - "Edge Cases"
      - "Analytics"
      - "Dependencies"
      - "Open Questions"
      - "Builder notes"
notes: "Rich context tempts the model to add Background/Context/etc. The spec is explicit: only the 4 required sections — nothing else."
