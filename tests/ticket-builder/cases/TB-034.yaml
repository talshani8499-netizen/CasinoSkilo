id: TB-034
category: handoff
title: "'Bet on it' + Atlassian MCP connected → calls jira-create-issue via correct tool regex"
references:
  - skill: ticket-builder
  - skill_md_lines: [157, 159]
preconditions:
  user_input: "add a VIP progress bar to the casino home"
  jira_context: |
    PR-470 / closed / "Casino home redesign" / component: home / file: components/home/banner.tsx
  confluence_context: null
  active_memory: null
  mcp_state: atlassian
  prior_artifacts: null
pass_criteria:
  - id: mcp-tool-lookup-pattern
    target: "jira-create-issue"
    expected_regex: 'mcp__.*(atlassian|jira).*'
    expected_verbs: ["createIssue", "createJiraIssue", "create_issue"]
  - id: markdown-saved-with-absolute-path
    folder: "tickets"
notes: "When user picks 'Bet on it' and atlassian MCP is connected, the skill must look up the create tool by the documented regex and pass project key + summary + markdown body."
