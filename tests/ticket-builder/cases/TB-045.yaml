id: TB-045
category: output-structure
title: "Inferred block 4-bullet cap with overflow marker when Phase A infers all 5 fields"
references:
  - skill: ticket-builder
  - skill_md_lines: [139, 139]
preconditions:
  user_input: "build a new feature: add a VIP progress bar on components/home/banner.tsx so end users (S1 players) can see their points-to-next-tier and drive deposits"
  jira_context: |
    PR-470 / closed / "Casino home redesign" / component: home / file: components/home/banner.tsx
    PR-481 / open / "Smartico LOYALTY_POINTS_UPDATE wiring" / component: integrations
    PR-602 / open / "VIP cashback v2" / component: vip / status: in QA
  confluence_context: |
    Page: "VIP Program v3 PRD" / decision: S1/S2 tier progress driven by Smartico events
  active_memory: null
  mcp_state: atlassian
pass_criteria:
  - id: phase-a-infers-field
    field: "Ticket type"
    expected_value: "New feature"
    must_cite_phrase: "new feature"
  - id: phase-a-infers-field
    field: "Main user"
    expected_value: "End user"
    must_cite_phrase: "end users"
  - id: phase-a-infers-field
    field: "Main goal"
    expected_value: "Business need"
    must_cite_phrase: "drive deposits"
  - id: phase-a-infers-field
    field: "Connection"
    expected_value: "Extends"
    must_cite_phrase: "components/home/banner.tsx"
  - id: phase-a-infers-field
    field: "Needs flow diagram"
    expected_value: "Yes"
    must_cite_phrase: "new feature"
  - id: inferred-block-cap-respected
    max_bullets: 4
    overflow_marker_required: true
    priority_order:
      - "Ticket type"
      - "Connection"
      - "Main user"
      - "Main goal"
      - "Needs flow diagram"
notes: "Line 139: when Phase A infers 5+ fields, the Inferred block shows only the top 4 by priority order (Ticket type > Connection > Main user > Main goal > Needs flow diagram) and appends an italic '_(+1 more inferred — see context for full set)_' line below the bullets. This input is rich enough that all 5 core fields infer cleanly."
